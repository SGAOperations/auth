// Product-side tickets, one Linear team per product. Same field shape as tickets-auth.mjs.
// Each team's epic is a single Linear Project: "SGAuth integration".

export const PRODUCT_TEAMS = {
  VAULTZ: {
    name: "VaultZ",
    epic: "SGAuth integration — VaultZ",
    label: "epic:sgauth-integration",
    phase: 2,
    description:
      "Replace the shared-passphrase gate with SGAuth sessions via the SDK. First product integrated; proves the SDK and the Neon-product guide.",
  },
  CHAMBERS: {
    name: "Chambers",
    epic: "SGAuth integration — Chambers",
    label: "epic:sgauth-integration",
    phase: 3,
    noLinear: true,
    description:
      "Chambers has no Linear team; these items are a Markdown checklist only (no CSV). Chambers is a Neon product (migration completes before integration). Replace Supabase Auth, live-role checks, and session revocation with SGAuth sessions and positions; users and password hashes are imported into SGAuth. The auth.users export is Eli's manual action item, not a ticket.",
  },
  APLIO: {
    name: "Aplio",
    epic: "SGAuth integration — Aplio",
    label: "epic:sgauth-integration",
    phase: 4,
    description:
      "Hard cutover from Aplio's local Better Auth (email OTP) to SGAuth via the SDK; users pre-imported as password-less accounts that set a password on first sign-in.",
  },
  SENATEPATH: {
    name: "SenatePath",
    epic: "SGAuth integration — SenatePath",
    label: "epic:sgauth-integration",
    phase: 4,
    description:
      "Migrate SenatePath's database from Supabase to Neon and gate the admin area with SGAuth positions.",
  },
  ATTENDANCE: {
    name: "Attendance Manager",
    epic: "SGAuth integration — Attendance Manager",
    label: "epic:sgauth-integration",
    phase: 4,
    description:
      "Decision pending from the team: either stay on Supabase and consume SGAuth via third-party auth (JWT trust), or move to Neon and use the SDK. Both paths are ticketed; only one will be executed.",
  },
};

export const PRODUCT_TICKETS = [
  // ───────────────────────── VaultZ ─────────────────────────
  {
    team: "VAULTZ",
    id: "VAULTZ-V01",
    priority: "High",
    estimate: 3,
    labels: ["sgauth", "backend"],
    deps: ["AUTH-T59", "AUTH-T80", "AUTH-T34"],
    title:
      "Install @sgaoperations/sgauth and protect all routes with the SGAuth proxy",
    description: `Add the SDK from public npm (no registry config), env vars (\`SGAUTH_URL\`, \`SGAUTH_COOKIE_NAME\`), and \`proxy.ts\` using \`createSgaProxy({ publicPaths: ['/api/uploadthing', '/health'] })\` (cookie-presence check only; real validation in server code via \`requireSession()\`). Unauthenticated requests redirect to \`https://auth.northeasternsga.com/login?redirect=<current URL>\`. Register \`https://vaultz.northeasternsga.com\` (and \`vaultz-dev.northeasternsga.com\`) in the SGAuth product registry first.`,
    acceptance: [
      "Visiting any VaultZ page without an SGAuth session redirects to SGAuth login and back to the original page after login.",
      "UploadThing callback route remains reachable without a session.",
    ],
  },
  {
    team: "VAULTZ",
    id: "VAULTZ-V02",
    priority: "High",
    estimate: 2,
    labels: ["sgauth", "backend"],
    deps: ["VAULTZ-V01"],
    title: "Remove the shared passphrase gate",
    description: `Delete \`lib/access-gate.ts\`, \`lib/actions/access.ts\`, the passphrase page, and the \`VAULTZ_ACCESS_CODE\`/\`VAULTZ_ACCESS_SECRET\` env vars after the cutover (keep behind a \`SGAUTH_ENABLED\` flag for one release so rollback is a flag flip, then delete).`,
    acceptance: [
      "No references to the passphrase remain after the flag is removed; the `vaultz_access` cookie is no longer set.",
    ],
  },
  {
    team: "VAULTZ",
    id: "VAULTZ-V03",
    priority: "Medium",
    estimate: 3,
    labels: ["sgauth", "data"],
    deps: ["VAULTZ-V01"],
    title: "Link VaultZ purchaser records to SGAuth user ids",
    description: `VaultZ's \`User\` table records purchasers (first/last), not logins. Add \`sgauthUserId String? @unique\` and \`email String?\`. On first authenticated request, find-or-create the purchaser row for the session user (name from SGAuth split into first/last as a best effort, editable). Admin UI to link legacy purchaser rows to SGAuth users by email. Purchases created by the current user default \`userId\` to their linked row.`,
    acceptance: [
      "A first-time SGAuth user gets exactly one purchaser row; re-login does not duplicate.",
      "Legacy rows can be linked once and the link is unique.",
    ],
  },
  {
    team: "VAULTZ",
    id: "VAULTZ-V04",
    priority: "High",
    estimate: 3,
    labels: ["sgauth", "permissions"],
    deps: ["VAULTZ-V01", "AUTH-T44"],
    title: "Position-based permissions map for VaultZ",
    description: `Create \`lib/permissions.ts\` mapping SGAuth position keys to VaultZ capabilities, using keys from the curated seed (\`prisma/seed/positions.json\` in SGAOperations/auth). Starting proposal for the VaultZ owner to confirm: \`vice-president-of-operational-affairs\`, \`comptroller\`, \`finance-manager\` → manage designations/budgets/transfers; \`student-body-president\`, \`executive-vice-president\` → read everything; any other position holder → create their own purchases; no positions → no access. Add a product role (e.g. \`vaultz-admin\`) in the SGAuth admin UI only if the curated offices do not cover a need. Gate server actions and pages with \`hasAnyPosition\`. Permissions are decided in VaultZ, not in SGAuth; SGAuth only supplies positions. Document the map in the README and agree the keys with the curated position list.`,
    acceptance: [
      "Every mutating server action checks a capability; a user without positions cannot create or edit anything (tests).",
      "The map is the only place position keys appear in VaultZ code.",
    ],
  },
  {
    team: "VAULTZ",
    id: "VAULTZ-V05",
    priority: "Low",
    estimate: 1,
    labels: ["sgauth", "ui"],
    deps: ["VAULTZ-V01"],
    title: "Account and sign-out links in the VaultZ header",
    description: `Show the session user's name, a link to \`accountUrl()\`, and a sign-out action using \`logoutUrl('https://vaultz.northeasternsga.com')\`.`,
    acceptance: [
      "Sign out from VaultZ logs the user out of every SGA product (verified on dev).",
    ],
  },
  {
    team: "VAULTZ",
    id: "VAULTZ-V06",
    priority: "Medium",
    estimate: 2,
    labels: ["sgauth", "infra"],
    deps: ["VAULTZ-V01", "AUTH-T34"],
    title: "Dev and preview topology for VaultZ",
    description: `Configure the Vercel branch domain \`vaultz-dev.northeasternsga.com\` pointing at the dev branch with \`SGAUTH_URL=https://auth-dev.northeasternsga.com\` and the dev cookie name; local dev on \`vaultz.sga.localhost:3001\` against local SGAuth. Document in the VaultZ README.`,
    acceptance: [
      "Preview on the branch domain shares the dev SGAuth session; ad-hoc vercel.app previews show a documented 'SSO unavailable on this host' page instead of a redirect loop.",
    ],
  },
  {
    team: "VAULTZ",
    id: "VAULTZ-V07",
    priority: "Medium",
    estimate: 1,
    labels: ["sgauth", "rollout"],
    deps: ["VAULTZ-V02", "VAULTZ-V04", "AUTH-T91"],
    title: "VaultZ cutover checklist and rollback",
    description: `Execute the per-product checklist from the rollout plan: registry entry, DNS, env vars, SDK version pinned, smoke test, announcement to finance users, \`SGAUTH_ENABLED\` rollback flag verified, 3-day support window.`,
    acceptance: [
      "Checklist completed; rollback rehearsed on dev by flipping the flag.",
    ],
  },
  {
    team: "VAULTZ",
    id: "VAULTZ-V08",
    priority: "Medium",
    estimate: 2,
    labels: ["sgauth", "testing"],
    deps: ["VAULTZ-V04"],
    title: "Tests for SGAuth guards and permissions in VaultZ",
    description: `Unit tests for the permissions map and integration tests (mocked SDK) for a protected page, a gated server action, and the purchaser link-on-first-login behavior.`,
    acceptance: [
      "Tests run in VaultZ CI and cover allow/deny for each capability.",
    ],
  },

  // ───────────────────────── Chambers ─────────────────────────
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C01",
    priority: "High",
    estimate: 3,
    labels: ["sgauth", "audit"],
    deps: [],
    title: "Inventory every auth and authorization touchpoint in Chambers",
    description: `List all uses of \`getAuthedUser\`, \`getAuthedUserWithLiveRoles\`, \`hasLiveAdmin\`, \`app_metadata\`, \`is_admin()\`, \`is_iems()\`, \`my_body_ids()\`, \`revoke_user_sessions\`, and any remaining Supabase Auth client calls (login, reset, password change) after the Neon migration. Produce a table: location → replacement (SGAuth session, position check, or Chambers-internal membership).`,
    acceptance: [
      "Inventory document merged in the Chambers repo and reviewed by the Chambers lead.",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C02",
    priority: "High",
    estimate: 5,
    labels: ["sgauth", "backend"],
    deps: ["CHAMBERS-C01", "AUTH-T59", "AUTH-T80"],
    title:
      "Replace Supabase Auth with the SGAuth SDK (proxy, session helpers, login removal)",
    description: `Install the SDK; add \`proxy.ts\` with public paths for kiosk/display pages (protected by \`DISPLAY_KEY\`), cron routes (\`CRON_SECRET\`), and the service worker assets; replace \`lib/auth.ts\`/\`lib/authorization.ts\` with thin wrappers over \`getSession()\`; delete \`LoginCard\`, forgot-password UI, and all \`supabase.auth.*\` calls; \`/login\` redirects to SGAuth with the current URL. Keep offline/network-retry UX for the session fetch failure case (show the existing offline message).`,
    acceptance: [
      "No `@supabase/*` auth imports remain; every previously protected route is protected by the proxy.",
      "Kiosk and cron routes work without a user session.",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C03",
    priority: "High",
    estimate: 3,
    labels: ["sgauth", "data"],
    deps: ["CHAMBERS-C02", "AUTH-T87"],
    title: "Key Chambers users by SGAuth user id",
    description: `Using the id-mapping file from the SGAuth import, migrate \`users.id\` (or add \`sgauth_user_id\` and repoint FKs) so the product's user row is keyed by SGAuth id; drop auth-only columns (\`sessions_revoked_at\`, password-related fields) and the dependency on \`auth.users\`. Create-on-first-login for users that exist in SGAuth but not in Chambers (e.g. self-registered students) with no memberships.`,
    acceptance: [
      "Every pre-existing Chambers user resolves to the same SGAuth user by email (verification script), and bookings history is intact.",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C04",
    priority: "High",
    estimate: 5,
    labels: ["sgauth", "permissions"],
    deps: ["CHAMBERS-C02", "AUTH-T44"],
    title:
      "Map admin_role / iems_role to SGAuth positions; keep body memberships internal",
    description: `Define \`lib/permissions.ts\`: Chambers admin capabilities derive from position keys: curated offices (proposal: \`vice-president-of-operational-affairs\`, \`speaker-of-the-senate\`, \`senate-operations-coordinator\`) plus product roles created in the SGAuth admin UI for roles that are not SGA offices (\`chambers-admin\`, \`chambers-iems\`); board/body memberships stay in Chambers tables keyed by SGAuth user id and continue to drive booking scopes. Replace \`hasLiveAdmin\`/\`is_admin()\` checks in app code with position checks; since Chambers is on Neon, RLS helpers are replaced by app-level checks in the data layer.`,
    acceptance: [
      "Every admin-only route and action checks positions; a user whose position is removed in SGAuth loses admin ability within 60 s (SDK cache) without any Chambers-side change.",
      "Body-scoped booking rules unchanged (existing tests pass).",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C05",
    priority: "Medium",
    estimate: 3,
    labels: ["sgauth", "cleanup"],
    deps: ["CHAMBERS-C04"],
    title: "Remove live-role-check and session-revocation mechanisms",
    description: `Delete \`getAuthedUserWithLiveRoles\`, \`sessions_revoked_at\`, \`revoke_user_sessions\`, and related migrations/tests; revocation and deactivation are now SGAuth's job (admin revoke in SGAuth → 401 from the SDK). Update the user-management admin pages in Chambers to link to the SGAuth admin UI for deactivation/positions instead of local toggles.`,
    acceptance: [
      "Chambers admin UI no longer offers role toggles that SGAuth owns; links open the SGAuth user page.",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C06",
    priority: "High",
    estimate: 1,
    labels: ["sgauth", "migration"],
    deps: [],
    title: "Approve the Chambers role-to-position mapping file",
    description: `(The auth.users export is Eli's separate manual action item and is not tracked here.) Review and approve the mapping from \`admin_role\` / \`iems_role\` / board memberships to curated position keys used by the SGAuth import (AUTH-T87).`,
    acceptance: [
      "Mapping file approved by the Chambers lead and the Primary Admin.",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C07",
    priority: "High",
    estimate: 2,
    labels: ["sgauth", "rollout"],
    deps: ["CHAMBERS-C03", "CHAMBERS-C04", "CHAMBERS-C05", "AUTH-T91"],
    title: "Chambers cutover, comms, and rollback plan",
    description: `Per-product checklist: registry entry for chambers.northeasternsga.com, env, SDK pin, import verified (sample users log in with existing passwords), announcement that passwords carry over, rollback plan (previous deployment + Supabase auth still intact for 7 days), 3-day support window.`,
    acceptance: [
      "Cutover completed with zero password resets required for imported users beyond expected stragglers; rollback rehearsed on dev.",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C08",
    priority: "Low",
    estimate: 1,
    labels: ["sgauth", "verification"],
    deps: ["CHAMBERS-C02"],
    title:
      "Verify kiosk display key, Slack reminders, and cron routes are unaffected",
    description: `Confirm the display pages, Slack bot posting, Resend flows, and GitHub-Actions-driven cron routes still work with the SGAuth proxy in place (they must be on the public path list).`,
    acceptance: [
      "Manual verification checklist completed on dev and production.",
    ],
  },
  {
    team: "CHAMBERS",
    id: "CHAMBERS-C09",
    priority: "Medium",
    estimate: 3,
    labels: ["sgauth", "testing"],
    deps: ["CHAMBERS-C04"],
    title: "Tests for SGAuth-based authorization in Chambers",
    description: `Replace tests that mocked Supabase JWT claims with tests that mock the SDK session; cover admin/iems/body-member/none for the main booking and admin routes.`,
    acceptance: [
      "Chambers CI green with the new tests; old Supabase auth tests removed.",
    ],
  },

  // ───────────────────────── Aplio ─────────────────────────
  {
    team: "APLIO",
    id: "APLIO-P01",
    priority: "High",
    estimate: 2,
    labels: ["sgauth", "migration"],
    deps: ["AUTH-T88"],
    title:
      "Export Aplio users for the SGAuth import and receive the id mapping",
    description: `Export \`User\` rows (id, email, name, isAdmin, deletedAt) to JSON for the SGAuth import; receive the SGAuth id-mapping file; verify every active Aplio user maps to an SGAuth user.`,
    acceptance: [
      "Mapping verified with a script; discrepancies resolved before cutover.",
    ],
  },
  {
    team: "APLIO",
    id: "APLIO-P02",
    priority: "High",
    estimate: 5,
    labels: ["sgauth", "backend"],
    deps: ["APLIO-P01", "AUTH-T59", "AUTH-T80"],
    title: "Replace local Better Auth with the SGAuth SDK",
    description: `Remove \`lib/auth/config.ts\` (emailOTP, rate limits), \`app/api/auth/[...path]\`, the OTP email template and Resend usage for OTP, the login/OTP pages, and \`resolveUser\`'s Better Auth branch; implement \`getCurrentUser\`/\`getOptionalUser\`/\`getDeactivatedSessionUser\` over the SDK. Keep Aplio's deactivation concept (\`deletedAt\`) as a product-level state. Drop the \`Session\`, \`Account\`, and \`Verification\` tables in a follow-up migration after cutover.`,
    acceptance: [
      "No `better-auth` dependency remains in Aplio; all `(auth)` routes are protected by the SGAuth proxy.",
      "Deactivated Aplio users still see the explanatory screen rather than a login loop.",
    ],
  },
  {
    team: "APLIO",
    id: "APLIO-P03",
    priority: "High",
    estimate: 5,
    labels: ["sgauth", "data"],
    deps: ["APLIO-P02"],
    title: "Key Aplio users by SGAuth user id",
    description: `Add \`sgauthUserId String @unique\` to \`User\`, backfill from the id-mapping file, and make all lookups go through it; create-on-first-login for new applicants (name/email from the session). Keep Aplio's own \`User.id\` as the FK target for the many audit relations to avoid rewriting every table. Remove \`neonAuthId\`.`,
    acceptance: [
      "Every existing application, answer, and email log still resolves to the right person after backfill (verification query).",
    ],
  },
  {
    team: "APLIO",
    id: "APLIO-P04",
    priority: "High",
    estimate: 2,
    labels: ["sgauth", "permissions"],
    deps: ["APLIO-P02", "AUTH-T44"],
    title:
      "Derive Aplio admin from an SGAuth position; managers stay product-level",
    description: `Replace \`User.isAdmin\` with a position check (e.g. \`aplio-admin\` or the relevant exec positions) in \`requireAdmin\`/\`requireAdminOr404\` and \`buildReviewablePositionWhere\`; position managers remain an Aplio relation. Provide a one-time script that assigns the \`aplio-admin\` position in SGAuth to current Aplio admins (via the SGAuth bulk assign API or CSV).`,
    acceptance: [
      "Admin pages gate on positions; the `isAdmin` column is removed after cutover.",
    ],
  },
  {
    team: "APLIO",
    id: "APLIO-P05",
    priority: "High",
    estimate: 3,
    labels: ["sgauth", "product"],
    deps: ["APLIO-P02"],
    title: "Applicant flow on SGAuth accounts (northeastern.edu required)",
    description: `Applicants now create SGAuth accounts via northeastern.edu self-sign-up with verification. Decision: every student has a northeastern.edu address, so no domain exception exists; applicants who previously used another address must use their Northeastern one. Update the apply entry points to redirect to SGAuth sign-up with a redirect back to the position, and add copy explaining the Northeastern-email requirement. Existing Aplio users with non-NU addresses are imported as legacy accounts (AUTH-T88) and keep working until an admin moves them to their NU address.`,
    acceptance: [
      "A new student can apply end-to-end: SGAuth sign-up → verify → land on the application form.",
      "Apply pages state the northeastern.edu requirement before the redirect.",
    ],
  },
  {
    team: "APLIO",
    id: "APLIO-P06",
    priority: "Low",
    estimate: 1,
    labels: ["sgauth", "dev"],
    deps: ["APLIO-P02", "AUTH-T34"],
    title: "Local dev bypass and preview topology",
    description: `Keep the dev-bypass user cookie for local only (never on any deployed host), and configure \`apply-dev.northeasternsga.com\` with the dev SGAuth URL and cookie name.`,
    acceptance: [
      "Bypass is a no-op on all Vercel deployments; preview branch domain shares the dev SSO session.",
    ],
  },
  {
    team: "APLIO",
    id: "APLIO-P07",
    priority: "High",
    estimate: 2,
    labels: ["sgauth", "rollout"],
    deps: ["APLIO-P03", "APLIO-P04", "APLIO-P05", "AUTH-T91"],
    title: "Aplio hard cutover outside an application window",
    description: `Schedule the cutover when no application cycle is open; users were pre-imported as password-less accounts (no invite blast); announce the change, explaining that the first sign-in asks for a new password by email; rollback = redeploy previous release (local Better Auth tables retained for 14 days); 3-day support window.`,
    acceptance: [
      "Cutover completed; no applicant lost draft access (verified with a sample of drafts).",
    ],
  },
  {
    team: "APLIO",
    id: "APLIO-P08",
    priority: "Medium",
    estimate: 3,
    labels: ["sgauth", "testing"],
    deps: ["APLIO-P04"],
    title: "Update Aplio tests for SDK-based auth",
    description: `Replace Better Auth session mocks with SDK session mocks; cover admin, manager, applicant, deactivated, and anonymous paths for guards and scopes.`,
    acceptance: ["Aplio CI green; guard coverage unchanged or higher."],
  },

  // ───────────────────────── SenatePath ─────────────────────────
  {
    team: "SENATEPATH",
    id: "SENATEPATH-S01",
    priority: "High",
    estimate: 5,
    labels: ["neon", "migration"],
    deps: [],
    title: "Migrate SenatePath's database from Supabase to Neon",
    description: `Create a Neon project/branches, dump and restore the Postgres schema and data (pg_dump from Supabase → psql into Neon), switch \`DATABASE_URL\`/\`DIRECT_URL\`, optionally upgrade Prisma 6 → 7 with the driver adapter, remove \`@supabase/*\` packages and Supabase Storage usage if any (or keep Storage only if truly needed and document it). SGAuth is Neon-only; SenatePath becomes a standard Neon product.`,
    acceptance: [
      "App runs against Neon in dev and prod; row counts match the Supabase source; no Supabase Auth code remains after S02.",
    ],
  },
  {
    team: "SENATEPATH",
    id: "SENATEPATH-S02",
    priority: "High",
    estimate: 3,
    labels: ["sgauth", "backend"],
    deps: ["SENATEPATH-S01", "AUTH-T59", "AUTH-T80"],
    title: "Gate the SenatePath admin area with SGAuth positions",
    description: `Install the SDK; protect \`/admin/**\` with the proxy; replace the Supabase login page with a redirect to SGAuth; gate admin capabilities on curated positions (proposal for the SenatePath owner to confirm: \`director-of-elections\`, \`vice-chair-of-elections\`, \`elections-board-member\`, \`speaker-of-the-senate\`). Public application/nomination forms remain unauthenticated as today.`,
    acceptance: [
      "Admin pages require an SGAuth session with the configured position; public forms unaffected.",
    ],
  },
  {
    team: "SENATEPATH",
    id: "SENATEPATH-S03",
    priority: "Medium",
    estimate: 2,
    labels: ["sgauth", "rollout"],
    deps: ["SENATEPATH-S02", "AUTH-T89"],
    title: "Import SenatePath admins into SGAuth and cut over",
    description: `Export admin emails for the SGAuth import; confirm they hold the gating position; run the per-product cutover checklist; delete Supabase project after a 14-day retention window.`,
    acceptance: [
      "All previous admins can log in via SGAuth; Supabase project decommissioned.",
    ],
  },
  {
    team: "SENATEPATH",
    id: "SENATEPATH-S04",
    priority: "Low",
    estimate: 2,
    labels: ["sgauth", "testing"],
    deps: ["SENATEPATH-S02"],
    title: "Tests for the SGAuth admin gate",
    description: `Integration tests (mocked SDK) for admin access with/without the position and for public routes.`,
    acceptance: ["Tests in CI cover allow/deny and public access."],
  },

  // ───────────────────────── Attendance Manager ─────────────────────────
  {
    team: "ATTENDANCE",
    id: "ATTENDANCE-M01",
    priority: "High",
    estimate: 1,
    labels: ["sgauth", "decision"],
    deps: [],
    title:
      "Decision: stay on Supabase (third-party auth) or move to Neon (SDK)",
    description: `Record the team's directive. Criteria: does Attendance Manager use Supabase-only features (Storage, Realtime, Edge Functions)? If not, moving to Neon is recommended (simpler, no JWT staleness, standard SDK). If yes, use the third-party auth path. Exactly one of M02 or M03 proceeds.`,
    acceptance: [
      "Decision documented in the repo README with rationale; the unused path's tickets are cancelled.",
    ],
  },
  {
    team: "ATTENDANCE",
    id: "ATTENDANCE-M02",
    priority: "High",
    estimate: 5,
    labels: ["sgauth", "supabase"],
    deps: ["ATTENDANCE-M01", "AUTH-T81", "AUTH-T60"],
    title: "(Supabase path) Consume SGAuth via Supabase third-party auth",
    description: `Follow the Supabase integration guide: register SGAuth as a third-party auth provider on the Attendance Supabase project via the Management API script; configure supabase-js with the SDK's access-token provider (server) and the proxy route (browser); replace \`supabaseAuthId\` with \`sgauthUserId\` on \`User\` and remove any \`auth.users\` FKs/triggers; rewrite RLS to \`auth.uid()\` and position claims (\`has_position('...')\`); remove Supabase login/signup pages and the custom middleware in favor of the SGAuth proxy; remove the unused \`password\` column. Note the ≤10-minute JWT staleness and the MAU billing line.`,
    acceptance: [
      "A user with the required position can read/write per RLS using an SGAuth token; a user without it is denied at the database.",
      "No `auth.users` dependency remains; Supabase login pages removed.",
    ],
  },
  {
    team: "ATTENDANCE",
    id: "ATTENDANCE-M03",
    priority: "High",
    estimate: 5,
    labels: ["sgauth", "neon"],
    deps: ["ATTENDANCE-M01", "AUTH-T59", "AUTH-T80"],
    title: "(Neon path) Migrate to Neon and integrate with the SDK",
    description: `Dump/restore Postgres to Neon, switch Prisma URLs, remove \`@supabase/*\`, install the SDK, protect routes with the proxy, key \`User\` by \`sgauthUserId\`, and move authorization from RLS to app-level checks using positions.`,
    acceptance: [
      "App runs on Neon with SGAuth sessions; row counts match; no Supabase code remains.",
    ],
  },
  {
    team: "ATTENDANCE",
    id: "ATTENDANCE-M04",
    priority: "Medium",
    estimate: 2,
    labels: ["sgauth", "permissions"],
    deps: ["ATTENDANCE-M01", "AUTH-T44"],
    title: "Map roles to SGAuth positions; keep NUID product-side",
    description: `Define the permissions map from position keys to Attendance roles (MEMBER/OFFICER/etc.); NUID stays in Attendance's \`User\` table keyed by SGAuth user id and is never sent to SGAuth.`,
    acceptance: [
      "Role checks derive from positions; NUID handling unchanged and documented.",
    ],
  },
  {
    team: "ATTENDANCE",
    id: "ATTENDANCE-M05",
    priority: "Medium",
    estimate: 2,
    labels: ["sgauth", "rollout"],
    deps: ["ATTENDANCE-M04", "AUTH-T89"],
    title: "User import and cutover",
    description: `Export users (email, names, role) for the SGAuth import, receive the id mapping, backfill \`sgauthUserId\`, run the per-product cutover checklist.`,
    acceptance: [
      "All active members map to SGAuth users; cutover completed with rollback rehearsed.",
    ],
  },
  {
    team: "ATTENDANCE",
    id: "ATTENDANCE-M06",
    priority: "Low",
    estimate: 2,
    labels: ["sgauth", "testing"],
    deps: ["ATTENDANCE-M04"],
    title: "Update auth and middleware tests",
    description: `Replace Supabase session mocks with SDK (or token) mocks in the existing auth-flow, middleware, and api-auth tests.`,
    acceptance: ["CI green; old Supabase auth tests removed."],
  },
];
